version: 2

build:
   os: "ubuntu-24.04"
   tools:
      python: "3.14"

python:
   install:
      - method: pip
        path: .

conda:
   environment: doc/environment.yml

sphinx:
  configuration: doc/environment.yml
